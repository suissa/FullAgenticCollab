import test from 'node:test'; import assert from 'node:assert/strict'; import {createState,createUser,createConsumer,createProduct,addStock} from '../../examples/ecommerce/src/domain.ts'; import {checkout} from '../../examples/ecommerce/src/checkout.ts';
test('checkout reserves stock captures payment and creates delivery',()=>{const s=createState(); createUser(s,{id:'u1',name:'Ada'}); createConsumer(s,{id:'c1',userId:'u1'}); createProduct(s,{id:'p1',name:'Book',priceCents:2500}); addStock(s,{productId:'p1',quantity:5}); const out=checkout(s,{paymentId:'pay1',deliveryId:'d1',consumerId:'c1',productId:'p1',quantity:2}); assert.equal(out.payment.status,'captured'); assert.equal(out.payment.amountCents,5000); assert.equal(out.delivery.status,'created'); assert.deepEqual(s.stock.get('p1'),{productId:'p1',available:3,reserved:2});});
test('checkout does not reserve when stock is insufficient',()=>{const s=createState(); createUser(s,{id:'u1',name:'Ada'}); createConsumer(s,{id:'c1',userId:'u1'}); createProduct(s,{id:'p1',name:'Book',priceCents:2500}); addStock(s,{productId:'p1',quantity:1}); assert.throws(()=>checkout(s,{paymentId:'pay1',deliveryId:'d1',consumerId:'c1',productId:'p1',quantity:2}),/insufficient/); assert.deepEqual(s.stock.get('p1'),{productId:'p1',available:1,reserved:0});});
