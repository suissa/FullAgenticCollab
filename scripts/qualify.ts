import {spawnSync} from 'node:child_process'; import {mkdirSync,readFileSync,writeFileSync} from 'node:fs'; import {execFileSync} from 'node:child_process';
function run(cmd:string,args:string[]){const r=spawnSync(cmd,args,{stdio:'inherit',env:process.env}); if(r.status!==0) process.exit(r.status??1);}
run('npm',['run','test:stage']); run('npm',['run','characterize']);
const revision=(()=>{try{return execFileSync('git',['rev-parse','HEAD'],{encoding:'utf8'}).trim();}catch{return 'unknown';}})(); const characterization=JSON.parse(readFileSync('.facop/evidence/characterization.json','utf8')); const passport={artifact:'examples/ecommerce',revision,generated_at:new Date().toISOString(),environment:{node:process.version,platform:process.platform,arch:process.arch},evidence:characterization.results}; mkdirSync('.facop/evidence',{recursive:true}); writeFileSync('.facop/evidence/passport.json',JSON.stringify(passport,null,2)); console.log(`QUALIFIED ${revision}: stage acceptance passed and characterization passport generated.`);
