# Consumers.createConsumer

Creates a buyer profile bound to an existing active User. This deliberately demonstrates a cross-entity invariant that is additionally verified by upstream integration tests.
